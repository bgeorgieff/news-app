const home = require('./home')
const article = require('./article')
const users = require('./users')
const comments = require('./comments')
const categories = require('./postCategory')

module.exports = {
  home,
  article,
  users,
  comments,
  categories
}